# Third-party notices

The production PDF editor currently contains no copied third-party source. It
uses Apple PDFKit and PencilKit APIs following the public
`PDFPageOverlayViewProvider` design presented in WWDC22.

The repository's PaperKit diagnostics also use Apple frameworks only. Public
MIT-licensed projects are retained as design references, not source
dependencies; see `docs/ARCHITECTURE.md`.
